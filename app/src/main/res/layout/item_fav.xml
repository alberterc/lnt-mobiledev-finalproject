<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:clickable="true"
        android:focusable="true"
        android:layout_margin="10dp"
        android:id="@+id/item_fav"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >

    <ImageView
            android:clickable="false"
            android:layout_width="100dp"
            android:layout_height="155dp"
            android:scaleType="centerCrop"
            android:id="@+id/image"
            android:src="@drawable/ic_launcher_background"
            android:layout_alignParentTop="true"
            android:layout_toStartOf="@+id/title" android:layout_marginEnd="30dp" android:layout_marginTop="2dp"
            android:layout_alignParentStart="true" android:contentDescription="@string/item_image_text"/>
    <TextView
            android:clickable="false"
            android:text="@string/item_title"
            android:textSize="17sp"
            android:gravity="center_vertical"
            android:layout_width="215dp"
            android:layout_height="90dp"
            android:id="@+id/title"
            android:layout_alignParentTop="true"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="35dp"/>
</RelativeLayout>